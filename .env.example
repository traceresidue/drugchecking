# Copy this file to .env and set your own password.
# .env is gitignored — never commit the actual password.
# On Vercel, set MAIN_PASSWORD in the project environment variables dashboard.
MAIN_PASSWORD=your-password-here
